import { ShowHidePassword } from './show-hide-password';

describe('ShowHidePassword', () => {
  it('should create an instance', () => {
    const directive = new ShowHidePassword();
    expect(directive).toBeTruthy();
  });
});
