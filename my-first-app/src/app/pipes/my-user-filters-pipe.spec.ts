import { MyUserFiltersPipe } from './my-user-filters-pipe';

describe('MyUserFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new MyUserFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
